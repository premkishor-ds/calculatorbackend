services:
  mongo:
    image: mongo:7
    container_name: vision-mongo
    ports:
      - "27017:27017"
    volumes:
      - vision_mongo_data:/data/db
    restart: unless-stopped

volumes:
  vision_mongo_data:
